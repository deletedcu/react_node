import React from 'react'

import routes from './routes'

/**
 * Root component, containing routes
 */

const Root = (props) => {
  return (
    <div>
      { routes }
    </div>
  )
}

export default Root
